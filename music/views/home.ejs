<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>仙人music</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">

<header class="p-4 bg-white shadow flex gap-2 sticky top-0 z-10">
<form action="/music/s" method="get" class="flex w-full gap-2">
<input name="q" value="<%= query === '__FAVORITES__' ? '' : query %>"
 placeholder="曲を探す"
 class="flex-1 border rounded-xl p-2">
<button class="bg-blue-500 text-white px-4 rounded-xl">🔍</button>
</form>
<button onclick="showFavorites()"
 class="bg-yellow-400 px-4 rounded-xl">⭐</button>
</header>

<main id="content" class="p-4 space-y-4 max-w-3xl mx-auto">

<% tracks.forEach(track => { %>
<div class="bg-white p-4 rounded-2xl shadow flex gap-4 items-center">
<img src="<%= track.artwork_url %>" class="w-20 h-20 rounded-xl">
<div class="flex-1">
<p class="font-bold"><%= track.title %></p>
<p class="text-sm text-gray-500"><%= track.username %></p>
</div>
<button onclick="playTrack('<%= track.id %>','<%= track.title %>','<%= track.artwork_url %>')"
 class="bg-blue-500 text-white w-12 h-12 rounded-full">▶</button>
<button onclick="toggleFavorite('<%= track.artwork_url %>','<%= track.title %>','<%= track.id %>')"
 class="text-2xl">❤️</button>
</div>
<% }) %>

</main>

<div id="player-bar"
 class="hidden fixed bottom-4 left-1/2 -translate-x-1/2
 bg-white rounded-2xl shadow-2xl border w-[95%] max-w-3xl p-3 z-50">
<p id="player-title" class="font-bold text-sm mb-1 truncate"></p>
<iframe id="player-frame" class="w-full h-24 rounded-xl"></iframe>
</div>

<script src="/app.js"></script>
</body>
</html>
